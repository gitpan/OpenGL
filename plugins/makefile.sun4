#!make

CC= gcc
OPTIMIZER= -g
CFLAGS=  $(OPTIMIZER) -DXP_UNIX -DPLUGIN_TRACE -I. -I$(ROOT)/usr/include

SRC= npunix.c npshell.c 
OBJ= npunix.o npshell.o

.c.o:
	$(CC) -c $(CFLAGS) $<

SHAREDTARGET=libopenglplugin.so

default all: $(SHAREDTARGET)

$(SHAREDTARGET): $(OBJ)
	$(LD) -o $(SHAREDTARGET) $(OBJ) $(LDFLAGS)

clean:
	$(RM) $(OBJ) $(SHAREDTARGET)






